export type RootLayoutProps = {
    children: React.ReactNode
}

export type PartBoxProps = {
    icon: string
    title: string
    subTitle: string
    activeOnPage: number
    checkmarkPage: number
    currentPage: number
}
